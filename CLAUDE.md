# ww2-atlas

第二次世界大戦（1937–1945）の日本語・時系列世界地図。**現場（地図）× 司令部（年表）× 国内（発表・報道・生活）** の 3 層を 1 本のタイムラインで同期する。

- 設計の正本: garden https://garden-five-inky.vercel.app/pages/2026-09-09-ww2-atlas-design/ （思考ログは vault `10_Thinking/2026-09-09-WW2-時系列世界地図サイト-設計.md`、実装引き継ぎは vault `10_Thinking/2026-09-09-ww2-atlas-P0-引き継ぎ.md`）
- データスキーマ: `docs/SCHEMA.md`（レコードは必ず `license` と `sources` を持つ）
- データ正本は `data/**/*.yaml`。`npm run data` で `public/data/` を生成。生成物はコミットしない
- 転載不可・要申請のソース（防衛研究所・有料新聞・NHK・Bundesarchiv 映像）は **本文を保存しない**。日付・自前要約・URL のみ（`license: link-only`）
- Wikipedia 由来テキストは `data/seed-wikipedia/` に隔離（`cc-by-sa`）
- LLM 下書きの要約は `verified: false`。原典確認したときだけ `true` にする
- 静的サイト（Vite + MapLibre GL）。サーバ・DB なし。Vercel にデプロイ
- **公開方針（2026-09-11 決定）: OSS として 2 段階で公開する**。Stage 1＝ライセンス 3 本（コード MIT／自前データ CC BY 4.0／`data/seed-wikipedia/` CC BY-SA 4.0）・README 日英・CONTRIBUTING・Issue テンプレ・CI を整えてリポを public に（noindex は維持）。Stage 2＝レコード毎の「未検証」バッジと「訂正する」Issue リンクを画面に付け、主要 50 件を verified にしてから noindex を外して発信（**別途 GO が要る**）。設計の詳細は garden 設計ページ §5、手順は vault 引き継ぎノート「次にやること 0」

## 実装状況（2026-09-09）

3 ペイン（地図・右ペイン・タイムライン）が動き、P0 のゴール 2 本が通っている:

- 総統指令第 21 号（1940-12-18）→ バルバロッサ作戦（Q83055）
- 大本営発表 1942-06-10（空母 1 隻喪失）→ ミッドウェー海戦（Q173034・実際は 4 隻）

| 層 | 件数 |
|---|---|
| 現場（Event） | **520**（Wikidata 2,060 件から選別） |
| 司令部（Decision） | **60**（独 16・日 17・連合 18・ソ 6・伊 3） |
| 国内（HomeFront） | 40（大本営発表 20・米報道 10・生活 10） |
| つながり（Link） | 66（うち discrepancy 付き 30） |
| 支配領域 | 36 キーフレーム（四半期＋転換日）・政体 325 |

**全レコードが `verified: false`（LLM 下書き）**。原典に当てたものだけ `true` にしていく。

### 地図の見せ方（設計ノートからの変更）

設計ノートは「司令部・国内は地図に置かない」だったが、**3 層とも地図に置く**ことにした。
会議も発表も「どこで起きたか」が分かる方が読める、という判断。
点の色は 5 分類（陸戦 / 海戦 / 空襲 / 会議・指令・降伏 / 発表・報道・生活）で、
降伏・休戦は戦闘でなく意思決定側に入れている。garden の設計ページは未更新。

### P0 で残っている割り切り

- ソ連領内の独軍占領地域は OHM に面が無く、独ソ国境は開戦時のまま描かれる
- 日本占領下の中国も同様（`中國` relation が 14 枠とも同一）
- ラテンアメリカは大半が 1941-42 に対枢軸宣戦しているが `neutral` のまま
- 前線ライン（FrontLine）は未実装
- 大陸命・大海令の命令番号は二次文献ベース。JACAR 原本との突き合わせは未了
- 降伏・休戦は Wikidata の取得条件（戦闘・軍事作戦のサブクラス）から構造的に落ちる。
  主要 4 件（仏 1940-06 / 伊 1943-09 / 独 1945-05 / 日 1945-09）は手で書いた

いずれも画面下部の注記に出している。解消は P1（PD 戦況図のトレース）。

### P1 でやったこと（2026-09-09）

- **取得の網を広げた**。戦闘・軍事作戦に加えて 首脳会談・条約 を P361 経由で、
  休戦・降伏・宣戦布告・講和条約・歴史的文書 を **P361 を使わず日付だけ**で取る経路を足した。
  外交系は WWII の一部（P361）として繋がっていないものが多く、旧来の条件では全部落ちていた
- **Event 154 → 520**、**Decision 30 → 60**（ソ連・イタリアのファイルを新設。
  独ソ不可侵条約が丸ごと抜けていた）
- **日付の精度バグを修正**。Wikidata の日付を `wdt:` で取ると年精度のものが 1/1 に丸まる。
  さらに日付と精度を別々に集計していたため「1937-01-01・精度 11」のような嘘の組み合わせが
  できていた（南京戦が 1 月に置かれていた）。日付と精度を同じ主張から採るようにし、
  年・月どまりのものは自動採用しない。**1/1 に固まった件数 42 → 1**

### 次に手を入れるなら

1. **支配領域の面**（最大の工数・未着手）。ソ連領内の独軍占領地域と日本占領下の中国が塗れない。
   PD の月次戦況図（〜1942-12）と米陸軍半月アトラス（1943-07〜）のトレースが要る。
   1943 年前半 6 ヶ月は両系列の隙間
2. `verified` を上げていく（Wikisource に全文がある 総統指令第 21 号・ポツダム宣言・
   終戦の詔書・真珠湾演説・独ソ不可侵条約 から）
3. 大本営発表を 20 回 → 846 回へ（JACAR 画像の OCR。**画像しか無く API も無いので自動化の目処が立っていない**）
4. Decision を 60 → 100、前線ラインの追加
